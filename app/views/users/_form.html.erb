<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :is_male %><br />
    <%= f.check_box :is_male %>
  </div>
  <div class="field">
    <%= f.label :orientation %><br />
    <%= f.text_field :orientation %>
  </div>
  <div class="field">
    <%= f.label :is_matched %><br />
    <%= f.check_box :is_matched %>
  </div>
  <div class="field">
    <%= f.label :matched_with %><br />
    <%= f.text_field :matched_with %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
